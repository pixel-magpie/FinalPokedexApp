import React from 'react'

export const PokeList = ({ img, name, types, onClick }) => {
  
  return (
    <div className="flex flex-col">
      <div className="justify-left p-8">
        <h2 className="text-2xl capitalize bg-white">Name</h2>
      </div>

    </div>
  )
}
